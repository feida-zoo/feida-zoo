# Audit 报告: pl_6932a56b — 飝龘动物园 README 更新

**审查人**: 毒刺 (Duci) 🦂  
**日期**: 2026-05-29  
**阶段**: audit  
**上游**: impl commit a3dd6de  
**改动范围**: README.md（+233 行重写）+ 4 张截图（docs/screenshots/）+ 测试更新（+27/-73）

---

## 一、安全审计

### 1.1 硬编码路径 ✅

README.md 中无 `/home/` 或 `/Users/` 硬编码路径。运行指南使用 `$FEIDA_ZOO_HOME` 环境变量。

### 1.2 敏感信息 ✅

- 无邮箱、无密码、无 API Key
- 端口号 18792/18793 已公开，附安全提示（仅绑定 127.0.0.1）

### 1.3 截图脱敏 ⚠️ P2

截图从 Dashboard 实时截取，可能包含 git 提交记录中的用户名 `feidada002@gmail.com`。但该邮箱已是 git commit 的 Author 字段（公开信息），且截图分辨率为 1280×800 缩略图，文字细节模糊，实际泄露风险低。

### 1.4 路径遍历风险 ✅

无用户输入路径构造，纯静态 Markdown 文档。

---

## 二、代码质量

### 2.1 README.md 内容审计

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 活跃/归档成员拆分 | ✅ | `### 活跃成员` + `### 已归档成员`，退出成员不在活跃区 |
| 核心守则完整 | ✅ | 8 条规则内联，无空链接 |
| 项目概述 | ✅ | 开篇即说明项目定位 |
| 技术栈 | ✅ | 6 层技术栈表格，含 SSE（verify 阶段移除的检查已恢复） |
| 目录结构 | ✅ | 6 个核心目录全覆盖 |
| 运行指南 | ✅ | 环境要求 + 安装 + 启动 + 访问地址 + API 端点 |
| Pipeline 7 阶段 | ✅ | 英文阶段名 + 中文标签 + ASCII 流程图 |
| 界面截图 ×4 | ✅ | 4 张 PNG 存在 docs/screenshots/，Markdown 引用路径正确 |
| 贡献指南 | ✅ | emoji 提交约定 + 分支策略 |
| 无硬编码路径 | ✅ | 正则验证无 `/home/` `/Users/` |

### 2.2 信息准确性校验

**API 端点**: README 列出 6 个端点，实际代码中存在 14 个。README 列出的是核心公开端点，未列出的（如 `/api/chat`、`/api/issues`、`/api/pipeline-status`）属内部/调试端点，不列出合理。✅

**成员职责**: Alpha 职责从「架构评审」更新为「开发实现」，准确反映 Weaver 退出后 Alpha 接管开发职责的现状。✅

**模型配置**: MiniMax-M2.7 / DeepSeek V4 Flash / GLM-5.1 与实际运行配置一致。✅

### 2.3 截图迁移 ✅

- `docs/pipeline/screenshot_*.png` 物理文件已删除
- `docs/screenshots/screenshot_*.png` 已 git add
- 旧目录截图从未被 git 跟踪（design commit 未 commit 截图文件）
- 无冗余残留

### 2.4 测试更新 ✅

`test_readme_update.py` 两处关键修改：

1. **`test_ex_members_not_in_active_table`**: 改为提取活跃子节文本，逐个检查已退出成员不在活跃表格中。逻辑精确，消除了旧版假阴性。✅
2. **`test_phase_stages_listed`**: 改为精确标题定位 `## Pipeline 工作流`，中英文双轨检查（en=7, cn=7, total=14）。逻辑健壮。✅

---

## 三、测试结果

```
27/27 passed (100%)
```

全部测试通过，无假阳性（已在 verify 阶段验证）。

---

## 四、性能风险

无。README 是纯静态 Markdown，截图总计 ~611KB，对仓库体积影响可接受。

---

## 五、P2 建议

1. **API 端点可扩展**: 当前仅列出 6 个核心端点，建议标注"更多端点见代码"或补充内部端点列表。非必须，不阻塞。

2. **截图脱敏**: 建议未来截图时遮挡 git 日志中的邮箱地址。当前风险极低，不阻塞。

---

## 六、判定

**PASS**

理由：
1. **安全**: 无硬编码路径、无敏感信息泄露、无路径遍历风险
2. **内容准确性**: 成员/职责/模型/Pipeline 阶段/API 端点均与实际代码一致
3. **Review/Verify 要求全覆盖**: 活跃/归档拆分、核心守则内联、截图稳定路径、Pipeline 7 阶段、无假阳性
4. **测试 27/27 通过**: 全绿，含边界检查
5. **截图迁移干净**: 无冗余残留
