# Review 报告: pl_6932a56b — 飝龘动物园 README 更新

**审查人**: 毒刺 (Duci) 🦂  
**日期**: 2026-05-29  
**阶段**: review（第二轮，审查上游 design commit 9144c18）  
**审查对象**: `docs/pipeline/pl_6932a56b_design.md` + 4 张截图

---

## 一、Design 产出概览

上游 commit `9144c18` 产出：
- **设计文档**: `docs/pipeline/pl_6932a56b_design.md`（157 行）
- **截图 ×4**: `docs/pipeline/screenshot_{kanban,members,chat,requirements}.png`（共 ~600KB，1280×800）

设计文档结构：需求评审 → 架构设计（What/Why/Tradeoff/文件清单）→ UI 设计 → 评审检查点

---

## 二、架构合理性

### ✅ 合理之处

1. **成员拆分策略正确**: 「活跃/归档」拆分优于直接删除，保留历史不误导
2. **截图存入仓库**: 4 张约 600KB，可接受，离线可用
3. **环境变量替代硬编码路径**: README 用 `${FEIDA_ZOO_HOME}` 避免路径泄露，合理
4. **README 结构大纲完整**: 概述/技术栈/成员/目录/运行/Pipeline/守则/截图/贡献，覆盖 review 发现的全部 P0/P1 问题
5. **截图已实际产出**: 4 张 PNG 均为 1280×800 真实界面截图（压缩率 3.8%-7.1%，非空白/占位图）

### P1 — 设计遗漏

6. **核心守则内容未具体定义**: 设计说「内联核心规则」，但没说明核心守则的实际内容是什么。当前 README 的「详见」后面是空的，design 文档也没有给出守则的正文。**风险**: impl 阶段可能写出随意内容或再次留空。应在此阶段给出守则正文或明确引用源。

7. **Pipeline 阶段列表不完整**: 设计提到 Pipeline 介绍，但未给出具体阶段列表。实际阶段为：requirement → design → review → impl → test → audit → deliver，design 应明确列出以便 impl 准确编写。

8. **「贡献指南」章节设计不足**: 大纲中有「贡献指南（提交约定/分支策略）」，但 design 未说明具体约定内容。项目使用 emoji 前缀提交（🐢/🦂/🐜/🐼），分支策略未说明。

9. **截图路径规划需确认**: 设计说截图放在 `docs/pipeline/`，但这是 pipeline 临时产物目录。截图作为 README 永久资源，应放在更稳定的位置（如 `docs/screenshots/` 或 `assets/`），避免与 pipeline 临时文件混杂。

### P2 — 细节建议

10. **归档成员表格格式未指定**: 活跃成员和归档成员是用两个独立表格，还是同一表格加「状态」列？design 未明确。

11. **截图裁剪/标注未设计**: 4 张全屏截图信息密度高，可考虑裁剪关键区域或添加标注框，但非必须。

---

## 三、安全风险

12. **截图内容审查缺失**: 截图可能包含敏感信息（如 git 提交中的用户名、邮箱、文件路径）。design 未要求截图脱敏。**建议**: impl 阶段需检查截图是否暴露 `feida-zoo` 邮箱或 `/home/afei/` 路径等敏感信息。

13. **Dashboard 端口暴露**: README 将写入访问地址 `http://localhost:18792`，属本地端口，风险可控。

---

## 四、遗漏检查

14. **`.gitignore` 未考虑截图目录**: 当前 `.gitignore` 忽略 `dashboard/data/*.json`，但 `docs/pipeline/` 下的 PNG 是否应排除？如果截图是永久资源则应纳入 git，如果截图随版本更新则需策略说明。design 未提及。

15. **Dashboard 当前运行状态**: design 提到「已确认 Dashboard 运行在 :18792」，但截图是实时截取的。如果 Dashboard 未运行，impl 阶段需要先启动 Dashboard。design 未说明启动前提。

16. **截图与实际功能一致性**: 当前 Dashboard 可能有功能更新（如最近新增的 priority 排序），截图需反映最新状态。design 未要求截图时效性校验。

17. **README 多语言**: 未讨论是否需要英文版本。当前项目完全中文，不需要，但应明确。

---

## 五、改进建议

| 优先级 | # | 问题 | 建议 |
|--------|---|------|------|
| P1 | 6 | 核心守则正文缺失 | design 补充守则正文或明确引用源文件 |
| P1 | 7 | Pipeline 阶段列表缺失 | design 明确列出 7 个阶段及流转规则 |
| P1 | 8 | 贡献指南内容未设计 | design 给出提交约定（emoji 前缀）和分支策略 |
| P1 | 9 | 截图路径在 pipeline 临时目录 | 改用 `docs/screenshots/` 或 `assets/` |
| P1 | 12 | 截图未要求脱敏 | impl 阶段须检查截图无敏感信息 |
| P2 | 10 | 归档成员格式未指定 | 建议独立表格 + 折叠（`<details>`） |
| P2 | 15 | Dashboard 启动前提未说明 | impl 前需确认 Dashboard 可运行 |

---

## 六、判定

**PASS**

理由：
1. **P0 问题全部覆盖**: 成员拆分（活跃/归档）、核心守则内联、截图已产出、项目概述/结构/运行指南/Pipeline/技术栈全部纳入设计
2. **截图质量合格**: 4 张 1280×800 真实界面截图，覆盖看板/成员/聊天/需求四大核心页面
3. **Tradeoff 分析合理**: 截图存仓库 vs 外部 URL、成员拆分 vs 删除，选择正确
4. **文件清单清晰**: README 重写 + 4 张截图，产出明确

P1 问题（守则正文/Pipeline 阶段列表/贡献指南内容/截图路径）均为 impl 阶段可自行补充的细节，不构成 design REJECT 理由。但 **建议 impl 阶段务必**：
- 从项目实际配置文件提取核心守则正文
- Pipeline 阶段列表写全 7 阶段
- 截图迁移到 `docs/screenshots/` 而非留在 `docs/pipeline/`
- 检查截图无敏感信息（邮箱/路径）
