# Test Report — pl_2070b427

**Task**: 成员管理界面人数不对  
**Requirement**: 6587c35b-17d0-4361-bae4-5e04ecea31fa  
**Tester**: Duci (🦂)  
**Date**: 2026-05-26  
**Input**: `framework/tests/ut/test_member_active_filter.py` + source code verification

---

## 测试执行

**环境**：Python 3.13.3 + pytest 9.0.3（临时 venv）  
**命令**：`/tmp/test_venv/bin/pytest framework/tests/ut/test_member_active_filter.py -v`

---

## 通过率：12/12 ✅ 100%

| # | 测试用例 | 结果 |
|---|---------|------|
| 1 | test_list_agents_returns_all_six | ✅ PASS |
| 2 | test_get_full_info_includes_status | ✅ PASS |
| 3 | test_active_three_retained | ✅ PASS |
| 4 | test_inactive_three_excluded | ✅ PASS |
| 5 | test_exact_active_count | ✅ PASS |
| 6 | test_fallback_path_same_filter | ✅ PASS |
| 7 | test_no_status_field_defaults_active | ✅ PASS |
| 8 | test_all_inactive_returns_empty | ✅ PASS |
| 9 | test_empty_members | ✅ PASS |
| 10 | test_monitor_should_skip_inactive | ✅ PASS |
| 11 | test_full_flow_active_filter | ✅ PASS |
| 12 | test_yaml_from_registry_filter_matches | ✅ PASS |

---

## 源码验证

**文件**：`dashboard/app_enhanced.py`

### 主路径 `_get_member_data()`（L1190~1196）
```python
# 跳过非活跃成员（已退出/retired）
member_status = meta.get("status", "active") if isinstance(meta, dict) else "active"
if member_status != "active":
    continue
```
✅ 已实现 status 过滤

### Fallback 路径（L1228~1234）
```python
# fallback 路径同样过滤非活跃成员
member_status = meta.get("status", "active") if isinstance(meta, dict) else "active"
if member_status != "active":
    continue
```
✅ Fallback 路径同样已实现 status 过滤（review 阶段 Issue 1 已修复）

---

## 遗留问题（不阻塞通过）

### 🟡 `_update_status()` 未过滤 inactive（Issue 2 未修复）

**位置**：`MemberStatusManager._update_status()` L225

**现状**：仍对全部 6 个成员（含 inactive）执行 `pgrep` 进程检测，每次轮询浪费 ~9s。

```python
for member_id in agent_ids:  # 包含 inactive 成员
    new_status[member_id] = self._detect_member_active_status(member_id)
```

**影响**：功能无影响（inactive 成员检测失败显示 offline 是预期行为），仅资源浪费。

**建议**：在 for 循环中加入 `status == "active"` 检查后再调用 `_detect_member_active_status()`。

---

## 结论：**pass**

**理由**：
- 12/12 测试用例全部通过
- 源码验证：主路径和 fallback 路径均已实现 `status == "active"` 过滤
- review 阶段指出的 Issue 1（fallback 缺过滤）已在实现阶段被修复
- Issue 2（`_update_status()` 资源浪费）不影响功能正确性

---

## 测试摘要

| 检查项 | 结论 |
|--------|------|
| 通过率 | ✅ 12/12 (100%) |
| 主路径过滤 | ✅ 已验证 |
| Fallback 过滤 | ✅ 已验证 |
| 边界用例覆盖 | ✅ 3 项（无 status/全部 inactive/空 members） |
| Issue 1 修复验证 | ✅ 已确认 |
| Issue 2 修复状态 | 🟡 未修复（不阻塞） |
| 最终结论 | **pass** |
