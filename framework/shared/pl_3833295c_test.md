# 测试报告：pl_3833295c — 动物园成员信息配置化

**Pipeline ID:** pl_3833295c
**Test phase:** 2026-05-22 04:14 GMT+8
**Tester:** Duci 🦂
**Input:** `framework/tests/mesh/test_zoo_registry_v2.py`（518 lines）
**Output:** 本文件

---

## 测试结果概览

**通过率：41/41（100%）**
**失败：0 个用例**

| 类别 | 通过 | 失败 |
|------|------|------|
| YAML Loading | 9 | 0 |
| Label Derivation | 6 | 0 |
| get_phase_agent() | 7 | 0 |
| get_model_display() | 5 | 0 |
| SessionRouter V2 | 5 | 0 |
| Lifecycle V2 | 4 | 0 |
| EdgeCases V2 | 5 | 0 |

---

## 验证测试

连续 3 次独立运行，100% 稳定（41/41 passed × 3）。

---

## 通过用例验证

| 测试 | 验证内容 |
|------|---------|
| `test_loads_members_from_yaml` | 成员从 YAML 正确加载 |
| `test_get_full_info_returns_all_fields` | get_full_info() 返回所有字段 |
| `test_get_info_downward_compatible` | 向下兼容接口正常 |
| `test_missing_yaml_file_raises` | 指定路径不存在时抛出 FileNotFoundError |
| `test_malformed_yaml_raises` | 损坏 YAML 抛出解析异常 |
| `test_reload_picks_up_new_members` | reload() 正确读取新成员 |
| `test_singleton_preserved` | 单例模式不同 yaml_path 互不污染 |
| `test_label_from_session_key` | session.key → label 推导正确 |
| `test_label_explicit_field_overrides_yaml` | 独立 label 字段优先级正确 |
| `test_label_missing_session_key` | 缺少 session.key 返回 None |
| `test_phase_to_alpha/duci/panda` | 阶段→成员映射正确 |
| `test_main_agent_excluded_from_phase_resolution` | 主 Agent 排除逻辑正确 |
| `test_non_main_agent_shows_model_alias` | 非主 Agent 模型 alias 解析正确 |
| `test_main_agent_shows_primary_model` | 主 Agent 显示 openclaw.json primary model |
| `test_model_display_no_openclaw_json` | openclaw.json 不存在时返回原始 model ID |
| `test_session_cache_still_works` | Session 缓存正常 |
| `test_empty_yaml_no_members` | 空 members YAML 返回空列表 |
| `test_yaml_with_non_required_fields` | 缺少可选字段的 agent 正常加载 |
| `test_multiple_agents_same_phase` | 多 Agent 共享 phase 时返回第一个 |

---

## 结论

**判定：pass**

41/41 测试用例全部通过，核心功能验证完毕：
- ZooRegistry YAML 驱动正常工作
- 单例模式下不同测试之间无污染
- label 推导、get_phase_agent、get_model_display 均正确
- YAML 错误处理（不存在/损坏）符合预期
- reload() 机制有效

---

**Reporter:** Duci 🦂
**Timestamp:** 2026-05-22 04:14 GMT+8
**Result:** pass